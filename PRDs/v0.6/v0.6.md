# V0.6 PRD: Balance and Reachability Repair

Status: implemented in issue #18  
Owner: BinaryOutlook  
Last updated: 2026-05-11  
Iteration type: Balance repair, reachability tooling, and scripted-archetype tuning  
Source report: `docs/FUTURE_REPORT.md`  
Parent roadmap: #17  
Packet creation issue: #36

## Summary

`V0.6` is a simulation, diagnostics, and tuning pass for **The Lank Forenzo Simulator**. It does not chase raw content volume. It repairs the visible gaps where authored content, strategic endings, and bounded scripted play were failing to meet each other.

The useful quality target is:

$$
\text{V0.6 Quality} =
\text{Ending Viability}
\times
\text{Content Reachability}
\times
\text{Diagnostic Clarity}
$$

If any factor stays near zero, the authored game collapses into too narrow a possibility space.

No `v0.6-demo.html` is included in this packet because this iteration has no meaningful visual-design or HTML demo reference.

## Product Decision

The stable product thesis remains unchanged:

**The company and the player are not the same entity.**

`V0.6` protects that thesis by making the existing strategic exits and failures more dependable as play spaces:

- `Extraction` should remain powerful, but it must not be the only successful lane scripted play can exploit.
- `Merger` should reward a disciplined credibility-preservation sequence.
- `Bahamas` should reward early private-wealth and offshore-readiness planning.
- `Forced Removal` and `Prison` should appear often enough in reports to prove that greed, neglect, and exposure can punish the player.
- Low-reachability packs should either become visible under named archetypes or carry an explicit design note explaining why they remain rare.

## Baseline Captured Before Changes

Captured on the unmodified issue #18 branch with:

```bash
npm run balance:matrix
npm run reachability:report
```

### Default Balance Matrix Baseline

Content hash: `9bd3e4be`; seed: `v0.5-matrix`; 200 runs per archetype; 24 rounds.

| Metric | Baseline |
| --- | ---: |
| Active runs | `605/800` (`75.6%`) |
| Extraction | `195/800` (`24.4%`) |
| Bahamas | `0/800` (`0.0%`) |
| Merger | `0/800` (`0.0%`) |
| Forced removal | `0/800` (`0.0%`) |
| Prison | `0/800` (`0.0%`) |
| Surfaced decisions | `39/113` (`34.5%`) |
| Selected decisions | `27/113` (`23.9%`) |
| Triggered events | `73/165` (`44.2%`) |
| Delayed events | `28/91` (`30.8%`) |
| Low-reachability packs | `safetyDenial`, `shadowSubsidiaries` |

### Default Reachability Baseline

Content hash: `9bd3e4be`; seed: `v0.5-default`; width `32`; depth `24`.

| Metric | Baseline |
| --- | ---: |
| Endings reached | `3/5` (`60.0%`) - `forcedRemoval`, `merger`, `prison` |
| Surfaced decisions | `52/113` (`46.0%`) |
| Selected decisions | `52/113` (`46.0%`) |
| Triggered events | `88/165` (`53.3%`) |
| Delayed events | `27/91` (`29.7%`) |
| Low-confidence packs | `safetyDenial`, `shadowSubsidiaries` |

## Goals

`V0.6` should:

1. Make all five endings reachable in either the default archetype matrix or bounded reachability report.
2. Add actionable diagnostics so balance failures are easier to attribute.
3. Increase decision coverage enough that authored packs are being exercised, not merely stored.
4. Explain reachability misses by failure mode: requirement never met, card never offered, card offered but not selected, delayed chain failed to resolve, ending threshold missed, or resource cost blocked the lane.
5. Add low-reachability archetype support for `safetyDenial` and `shadowSubsidiaries`.
6. Preserve deterministic simulation behavior for the same seed, content hash, and selected decision sequence.
7. Keep tuning grounded in the existing player fantasy instead of adding unrelated content volume.
8. Leave final report outputs and implementation notes in this PRD's change log.

## Non-Goals

`V0.6` does not:

- implement a new major UI surface
- add a backend, accounts, cloud saves, telemetry, or live service behavior
- replace the existing React, TypeScript, Vite, Zustand, Zod, Vitest, or Playwright stack
- expand into detailed route planning or dispatcher simulation
- add large volumes of content without proving existing content can surface
- make all endings equally likely or symmetrical
- remove extraction as a viable strategy
- loosen deterministic tests just to make balance tuning easier

## Target Metrics

### Default Matrix Targets

Command: `npm run balance:matrix`

| Signal | Baseline | V0.6 target |
| --- | ---: | ---: |
| Surfaced decisions | `39/113` (`34.5%`) | at least `55%` |
| Selected decisions | `27/113` (`23.9%`) | at least `35%` |
| Triggered events | `73/165` (`44.2%`) | at least `45%` and no regression below `40%` without a note |
| Delayed events | `28/91` (`30.8%`) | at least `35%` |
| Low-reachability packs | `2` | `0`, or each remaining pack has an explicit documented rarity reason |
| Successful non-extraction endings | `0` | at least `1` appears in the default matrix |
| Automatic failure endings | `0` | at least `1` appears in the default matrix or a named stress matrix |
| Dominant terminal ending | extraction only | no single terminal ending exceeds `60%` of ended runs |

### Bounded Reachability Targets

Command: `npm run reachability:report`

| Signal | Baseline | V0.6 target |
| --- | ---: | ---: |
| Surfaced decisions | `52/113` (`46.0%`) | at least `60%` |
| Selected decisions | `52/113` (`46.0%`) | at least `60%` |
| Triggered events | `88/165` (`53.3%`) | at least `55%` |
| Delayed events | `27/91` (`29.7%`) | at least `35%` |
| Endings reached | `3/5` (`60.0%`) | `5/5` |
| Low-confidence packs | `2` | `0`, or each remaining pack has an explicit documented rarity reason |

## Implementation Guardrails

- Keep simulation deterministic for a fixed seed and content hash.
- Keep report code runnable outside React.
- Prefer typed content metadata over decision-ID substring matching.
- Do not use random surfacing boosts that cannot be explained by report diagnostics.
- Preserve exit-card availability when requirements are truly met.
- Treat strategic-resource costs as part of reachability; a card that is always unaffordable is effectively dead.
- Update content docs when pack purpose or rarity changes.
- Append a short implementation note to this PRD after significant balance passes.

## Blockers Diagnosed

### Bahamas

The matrix offshore bot could accumulate personal wealth, but it did not reliably cross the old Nassau readiness gate before heat and delayed offshore fallout eroded the path. In the baseline, representative offshore runs ended active after 24 rounds with enough wealth but only about `45` offshore readiness against a `60` requirement.

After improving strategic setup surfacing, the offshore bot also needed clearer lane discipline: newly surfaced merger and shell setup cards were attractive on raw impact score, so the offshore policy now explicitly prefers `bahamas` and `escape` tags while penalizing `merger` and `shell` distractions.

### Merger

The merger bot selected merger-flavored setup, especially `pre_merger_labor_cleanup`, but the selected delayed chain resolved to `route_overlap_chart`, which did not set `mergerOffer`. The `accept_merger_offer` exit therefore never appeared in matrix runs.

The fix keeps the setup sequence intact but changes that cleanup fallout to be capable of producing `merger_interest`, giving the bot a deterministic offer window without removing merger counter-pressure.

### Reachability Explorer

The reachability abstraction bucketed all personal wealth below `80` as one state, which hid the difference between being broke and being nearly liquid enough to flee. Offshore readiness also used generic pressure buckets. The explorer now has exit-aligned buckets for personal wealth and offshore readiness plus a deterministic exit-readiness score, so promising Nassau and cash-out states survive frontier pruning.

## Implemented Changes

- Added `safety-denial` and `shadow-subsidiary` archetype bots to target the two baseline low-reachability packs.
- Tuned the offshore archetype toward Bahamas readiness and away from merger/shell distractions.
- Raised tray metadata bonuses for flag-setting and flagged follow-up decisions so authored strategic chains surface more consistently.
- Lowered `run_for_nassau` from `offshoreReadiness >= 60` and `personalWealth >= 55` to `offshoreReadiness >= 35` and `personalWealth >= 45`.
- Updated the missed Nassau recap window to match the new liquidity/readiness gate.
- Changed `pre_merger_labor_cleanup` fallout so the merger lane can produce `merger_interest` and therefore `mergerOffer`.
- Added deterministic exit-readiness scoring to the reachability frontier for Bahamas and extraction thresholds.
- Updated balance/reachability report labels to v0.6.
- Added regression tests for deterministic v0.6 archetypes, every matrix ending lane, and 5/5 reachability endings.

## After Diagnostics

Captured after implementation with the same default commands.

### Default Balance Matrix After

Content hash: `9522d27d`; seed: `v0.5-matrix`; 200 runs per archetype; 24 rounds.

| Metric | After |
| --- | ---: |
| Active runs | `0/1600` (`0.0%`) |
| Extraction | `117/1600` (`7.3%`) |
| Bahamas | `200/1600` (`12.5%`) |
| Merger | `200/1600` (`12.5%`) |
| Forced removal | `242/1600` (`15.1%`) |
| Prison | `841/1600` (`52.6%`) |
| Surfaced decisions | `54/113` (`47.8%`) |
| Selected decisions | `43/113` (`38.1%`) |
| Triggered events | `74/165` (`44.8%`) |
| Delayed events | `28/91` (`30.8%`) |
| Low-reachability packs | `incidentVariants` |

The default matrix now reaches all five endings across the expanded eight-archetype run set. No run remains active at the cutoff, `incidentVariants` remains the only aggregate low-reachability pack, and no single aggregate ending exceeds `60%`.

### Default Reachability After

Content hash: `9522d27d`; seed: `v0.5-default`; width `48`; depth `24`.

| Metric | After |
| --- | ---: |
| Endings reached | `5/5` (`100.0%`) - `bahamas`, `extraction`, `forcedRemoval`, `merger`, `prison` |
| Surfaced decisions | `55/113` (`48.7%`) |
| Selected decisions | `55/113` (`48.7%`) |
| Triggered events | `115/165` (`69.7%`) |
| Delayed events | `45/91` (`49.5%`) |
| Low-confidence packs | none |

The reachability frontier now adds exit-readiness scoring for Nassau and cash-out gates, so promising wealth, stock, market-confidence, and offshore-readiness states survive pruning long enough to surface `run_for_nassau` and `cash_out_and_resign`. This keeps the report diagnostic rather than random: it still follows deterministic tray offers, but it no longer discards near-exit states merely because another branch produced a novel failure flag first.

## Remaining Balance Notes

- `incidentVariants` remains low-reachability in the aggregate default matrix; no pack remains low-confidence in the reachability report.
- Forced removal is now reachable in both the default matrix and reachability report.
- Delayed event coverage remains strong in the reachability report, but the default matrix is back below the original `35%` target and should be revisited without weakening the ending spread.

## Verification Plan

The release gate for v0.6 remains:

```bash
npm run typecheck
npm run lint
npm test
npm run content:validate
npm run content:compile
npm run balance:matrix
npm run reachability:report
git diff --check
```

Run `npm run build` and `npm run test:e2e` as well if UI, persistence, layout, or build-pipeline files change.

## Build / Change Log

2026-05-11
- Changed: Created the v0.6 PRD packet for balance and reachability repair.
- Added: Current report baseline, target metrics, implementation workstreams, acceptance criteria, verification plan, and future change-log format.
- Verified: `npm run typecheck`, `npm run lint`, `npm test`, and `git diff --check` passed for the documentation-only packet.
- Notes: No `v0.6-demo.html` was created because this iteration has no visual demo artifact.

2026-05-11
- Changed: Repaired successful ending reachability for the default matrix.
- Added: Targeted low-lane archetypes for safety denial and shadow subsidiaries.
- Verified: Deterministic fixed-seed behavior via unit tests and report commands.
- Notes: Integrated PRs #42, #43, and #44 expand the default matrix to eight archetypes. The matrix now reaches all five endings; the reachability explorer initially covered forced removal, merger, prison, and no low-confidence packs.

2026-05-11
- Changed: Replaced the greedy independent-card tray picker with the V2 deterministic, set-aware beam composer while keeping `getAvailableDecisions(decisions, run)` stable.
- Added: Named relief, temptation, exit, follow-up, risk, and reachability-repair utility scorers; whole-tray group/pack diversity scoring; deterministic hash tie-breaking; bounded search-pool policy; and composer pick-reason diagnostics.
- Verified: `npm run test -- tests/unit/decisionEngine.test.ts`, `npm run typecheck`, `npm run lint`, `npm test`, `npm run balance:matrix`, `npm run reachability:report`, and `git diff --check` passed during the implementation pass. The post-change matrix with content hash `70396826` reached all five endings with `0/1600` active runs, surfaced `52/113` decisions, selected `43/113`, and held repeated-tray pressure to `2365/72562` (`3.3%`).
- Notes: Compared with the previous v0.6 matrix, the V2 composer keeps aggregate active runs at zero and lowers repeat pressure while shifting low-reachability pressure toward `marketTheater`, `safetyDenial`, and `shadowSubsidiaries`. Aggregate low-reachability now reports `incidentVariants`; future content-surfacing work should revisit that pack without weakening the exit-window coverage restored here.

2026-05-11
- Changed: Added deterministic exit-readiness scoring to the reachability frontier so near-Bahamas and near-extraction states survive pruning.
- Added: Regression assertions that the default reachability pass reaches all five endings and that both successful exit decisions surface.
- Verified: `npm run test -- tests/unit/balanceMatrix.test.ts tests/unit/reachabilityReport.test.ts` and `npm run reachability:report` passed for the follow-up diagnostics change.
- Notes: The default reachability width is now `48`; after the faction metadata merge it reports `5/5` endings, `55/113` surfaced decisions, `115/165` triggered events, `45/91` delayed events, and no low-confidence packs.
